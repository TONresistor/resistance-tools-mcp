interface:
  display_name: "Sites"
  short_description: "Publish and manage Resistance Tools sites"
  default_prompt: "Use $sites to publish or manage a site with Resistance Tools."

dependencies:
  tools:
    - type: "mcp"
      value: "resistance-tools"
      description: "Hosted Resistance Tools MCP"
      transport: "streamable_http"
      url: "https://app.resistance.dog/api/mcp"

policy:
  allow_implicit_invocation: true
