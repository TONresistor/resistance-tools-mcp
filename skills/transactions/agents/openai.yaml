interface:
  display_name: "Transactions"
  short_description: "Prepare and verify wallet-confirmed actions"
  default_prompt: "Use $transactions to prepare and verify this wallet-confirmed action."

dependencies:
  tools:
    - type: "mcp"
      value: "resistance-tools"
      description: "Hosted Resistance Tools MCP"
      transport: "streamable_http"
      url: "https://app.resistance.dog/api/mcp"

policy:
  allow_implicit_invocation: true
