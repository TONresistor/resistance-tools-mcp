interface:
  display_name: "Domains"
  short_description: "Manage TON DNS and Subdomain workflows"
  default_prompt: "Use $domains to inspect or manage a TON domain or Subdomain."

dependencies:
  tools:
    - type: "mcp"
      value: "resistance-tools"
      description: "Hosted Resistance Tools MCP"
      transport: "streamable_http"
      url: "https://app.resistance.dog/api/mcp"

policy:
  allow_implicit_invocation: true
