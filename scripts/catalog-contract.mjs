export const expectedToolContracts = [
  ["auth.status", "public", "local-read"],
  ["auth.policy", "public", "local-read"],
  ["wallet.me", "wallet:read", "remote-read"],
  ["mcp.access.list", "mcp:read", "remote-read"],
  ["mcp.audit.list", "mcp:read", "remote-read"],
  ["mcp.audit.summary", "mcp:read", "remote-read"],
  ["mcp.access.revoke_consent", "mcp:revoke", "destructive"],
  ["sites.list", "sites:read", "remote-read"],
  ["sites.get_content", "sites:read", "remote-read"],
  ["sites.list_releases", "sites:read", "remote-read"],
  ["sites.publish_files", "sites:write", "destructive"],
  ["sites.publish_template", "sites:write", "destructive"],
  ["sites.rollback", "sites:rollback", "destructive"],
  ["sites.delete", "sites:delete", "destructive"],
  ["deployments.list", "deployments:read", "remote-read"],
  ["dns.lookup", "dns:read", "remote-read"],
  ["domains.list", "dns:read", "remote-read"],
  ["domains.records", "dns:read", "remote-read"],
  ["dns.prepare_record_tx", "dns:prepare_tx", "remote-read"],
  ["dns.prepare_site_record_tx", "dns:prepare_tx", "remote-read"],
  ["media.upload_image", "media:write", "write"],
  ["storage.list_bags", "storage:read", "remote-read"],
  ["storage.bag_details", "storage:read", "remote-read"],
  ["storage.create_bag", "storage:write", "write"],
  ["storage.pin_bag", "storage:write", "write"],
  ["storage.delete_bag", "storage:delete", "destructive"],
].map(([name, scope, annotations]) => ({ name, scope, annotations }));

export const annotationProfiles = {
  "local-read": {
    readOnlyHint: true,
    destructiveHint: false,
    idempotentHint: true,
    openWorldHint: false,
  },
  "remote-read": {
    readOnlyHint: true,
    destructiveHint: false,
    idempotentHint: true,
    openWorldHint: true,
  },
  write: {
    readOnlyHint: false,
    destructiveHint: false,
    idempotentHint: false,
    openWorldHint: true,
  },
  destructive: {
    readOnlyHint: false,
    destructiveHint: true,
    idempotentHint: false,
    openWorldHint: true,
  },
};

export const allScopes = [
  "wallet:read",
  "sites:read",
  "sites:write",
  "sites:rollback",
  "sites:delete",
  "deployments:read",
  "dns:read",
  "dns:prepare_tx",
  "storage:read",
  "storage:write",
  "storage:delete",
  "media:write",
  "mcp:read",
  "mcp:revoke",
];
